# 更新记录

## v1.2.0（未发布）

- 新增标签拖拽排序：英文标签区可按住标签直接拖动，明细表每行左侧提供 `⠿` 手柄（含插入指示线与边缘自动滚动），选中标签后支持 `Alt+↑` / `Alt+↓` 键盘微调；三处入口共用同一套 `Ctrl+Z` 撤销历史。
- 排序按“提取—插入”建模：保留原有逗号、空格与换行排版，多行结构不被合并；自然语言块整体移动，内部不拆分；重复标签按出现位置独立移动。
- 明细表筛选视图（未知／机器译／个人词库／收藏）与自然语言、指令模式下不启用拖拽，避免局部顺序被误解为完整顺序。
- 新增 `tests/test_reorder.mjs`：覆盖基本移动、往返一致、跨换行、跨 `BREAK`/`AND`、权重与 LoRA 保留、重复标签、尾逗号、混合模式自然语言块不可拆等 30+ 断言。
- 新增标签临时隐藏：明细表行内与英文标签 hover 处的眼睛图标可把标签移出实际输出文本（模型完全看不到），标签进入“已隐藏”区域后可逐个或全部恢复到原位置，适合做“有无该标签”的对比出图。
- 隐藏清单保存在 `node.properties.bpiHiddenTags`，随工作流文件保存；撤销条目同时快照清单，`Ctrl+Z` 回滚文本与清单，不会产生无法清掉的恢复项。权重标签、`<lora:>`、`BREAK` 和自然语言块均可整体隐藏与恢复。
- 新增 `tests/test_hide.mjs`：覆盖隐藏—恢复往返、插回最前与锚点后、锚点失效留末尾、尾部换行与尾逗号保留、空文本、权重/LoRA/BREAK/@ 原样恢复、重复标签锚点、cursor 定位等断言。

## v1.1.0（2026-09-18）

- 新增面向全部启用词库的概念模糊搜索、40 项分页、“搜索更多”和结果结束提示。
- 未知标签可直接搜索候选或手动加入个人词库。
- 新增 LM Studio 本地预设并修正兼容接口地址拼接。
- 中文／混合文本编辑区支持高度调整、持久化和展开编辑。
- “按官方顺序整理”支持分类换行，保留自然语言、权重和分段结构。
- 修复“搜索更多”后列表跳回顶部；同一查询会保持原浏览位置。
- 分类整理后的中文联动区改为带分类名称的两列表格，分类名称不会写入实际提示词。
- 英文实际输出区新增二次确认清空按钮；确认清空后按钮变为“撤销”，同时支持 `Ctrl+Z`。中文／混合草稿区不设置重复的清空入口，返回联动即可查看英文输出对应的中文视图。
- 适配 ComfyUI 0.36.0 现代节点布局接口；顶部 ComfyUI 原始英文文本框保持可见，标签管理展开／收起时节点重新适配内容高度。
- 发布包不再携带空个人词库或默认运行设置，避免覆盖升级时清空 `user_tags.json`、词库启停选择和大型词库选择；构建脚本新增旧版覆盖升级哨兵测试。

## v1.0.0

- 首个公开正式版本。
