# 更新记录

## v1.2.0（2026-09-23）

- 翻译服务与 AI 后端解耦（schema v3）：翻译/解释可选纯词库 / 百度翻译 / AI；"翻译并优化"与"优化为 Anima"恒走 AI。AI 与百度密钥独立留存，切换翻译服务互不清空——只有 AI 后端/地址变更或换机器时才清 AI Key，百度密钥只在显式清除时才清。
- 配置面板重写为三段式：翻译服务下拉、百度翻译（APP ID + 密钥，始终可编辑）、AI 服务（后端 + 地址 + 模型 + Key + 温度 + 超时 + LM Studio 预设）。
- 旧 v2 配置自动迁移至 v3：`provider=openai_compatible/ollama` → `translate_service=ai`、`provider=baidu` → `translate_service=baidu`；旧 `base_url/model/api_key/temperature/timeout_seconds` 搬入 `ai_*` 槽；旧 `credential_binding` 改名 `ai_credential_binding`。
- 修复 `clear_ai_api_key` / `clear_baidu_secret_key` 在 payload 未同时包含对应密钥字段时不生效的嵌套条件 bug。
- 修复节点高度棘轮上涨：ComfyUI `text` 控件的 `computedHeight` 是单调累加器，每次写值都会让节点变大一点。改用原生 `text` 控件的 `scrollHeight@auto` 作为稳定基线，不再读 `widget.y`，节点高度在多次写值后锁定不变。
- `inspectorNodeTargetHeight` 签名简化为 `{baseHeight, targetWidgetHeight, fallbackBaseHeight, minimumHeight}`，移除旧 `widgetY`/`nodeHeight`/`currentWidgetHeight` 等参数。

- 新增标签拖拽排序：英文标签区可按住标签直接拖动，明细表每行左侧提供 `⠿` 手柄（含插入指示线与边缘自动滚动），选中标签后支持 `Alt+↑` / `Alt+↓` 键盘微调；三处入口共用同一套 `Ctrl+Z` 撤销历史。
- 排序按“提取—插入”建模：保留原有逗号、空格与换行排版，多行结构不被合并；自然语言块整体移动，内部不拆分；重复标签按出现位置独立移动。
- 明细表筛选视图（未知／机器译／个人词库／收藏）与自然语言、指令模式下不启用拖拽，避免局部顺序被误解为完整顺序。
- 新增 `tests/test_reorder.mjs`：覆盖基本移动、往返一致、跨换行、跨 `BREAK`/`AND`、权重与 LoRA 保留、重复标签、尾逗号、混合模式自然语言块不可拆等 30+ 断言。
- 新增标签临时隐藏：明细表行内与英文标签 hover 处的眼睛图标可把标签移出实际输出文本（模型完全看不到），标签进入“已隐藏”区域后可逐个或全部恢复到原位置，适合做“有无该标签”的对比出图。
- 隐藏清单保存在 `node.properties.bpiHiddenTags`，随工作流文件保存；撤销条目同时快照清单，`Ctrl+Z` 回滚文本与清单，不会产生无法清掉的恢复项。权重标签、`<lora:>`、`BREAK` 和自然语言块均可整体隐藏与恢复。
- 新增 `tests/test_hide.mjs`：覆盖隐藏—恢复往返、插回最前与锚点后、锚点失效留末尾、尾部换行与尾逗号保留、空文本、权重/LoRA/BREAK/@ 原样恢复、重复标签锚点、cursor 定位等断言。
- 新增上游输入与暂停确认：节点左侧 `prompt` 输入口可接任意输出文本的上游节点；接管开启时运行到本节点会挂起，把上游文本导入节点等待整理，点「继续运行」后才把结果交给下游，期间下游不会执行。关闭接管则原样透传。
- 暂停复用 ComfyUI 的异步节点机制（`async def` 的节点函数未完成时执行器会把节点置为 pending 并阻塞下游），不是轮询等待；等待中响应 ComfyUI 的中断，也可点「放弃」终止。无人接管时（API 提交、无浏览器）约 20 秒后自动透传，不会永久卡住。
- 新增导入策略下拉（内容变化时／每次运行／仅首次），默认「内容变化时」，避免重新运行取回的上游文本覆盖已整理的内容。
- 上游文本无论接管还是透传都会同步进节点，保证节点显示的就是本次实际输出（此前透传时节点与侧边栏都看不到上游内容）。
- 修复侧边栏「标签管理」分页挂载节点界面后明细表可能为空：挂载完成即触发一次同步渲染；未指定节点时优先选中画布上当前选中的节点，而不是永远第一个。
- 自定义 socket 事件改为幂等注册（setup 与节点创建各试一次）——ComfyUI 前端只分发已注册过的未知事件类型，注册晚了就收不到。
- 修复渲染时抛 `sourceLabel is not a function`：新增的上游输入条里用了与模块顶层 `sourceLabel()`（标签来源文案）同名的局部变量，把它遮蔽掉了，导致整个渲染中断、明细表与侧边栏都空白。
- 修复节点高度计算抛 `targetWidgetHeight is not defined`：`inspectorNodeTargetHeight` 的参数字段被当成变量使用，尺寸调整因此失败。
- 新增 `tests/test_upstream_gate.py`：覆盖放行文本、无文本回退上游、无人接管超时、ack 延长时限、取消中断、中断响应、节点无上游直通、节点无人接管透传、节点返回编辑后文本等 9 个用例。
- 新增提示词收藏：节点「英文提示词（实际输出）」旁新增「收藏」按钮，命名后可选配一张出图截图保存；侧边栏新增「收藏」分页，支持搜索、备注、复制、载入节点、删除与导入导出。
- 收藏数据存在 ComfyUI 用户目录 `user/bilingual-prompt-inspector/saved_prompts/`（`index.json` + 独立图片文件），不写入插件目录，重装插件不丢数据；单图上限 5 MB，最多 2000 条。
- 收藏不记录工作流信息；「载入节点」先弹窗确认再覆盖节点文本，不进 `Ctrl+Z` 撤销栈。侧边栏「新建」在没有选中节点时提供节点选择器兜底。
- 新增 `tests/test_saved_prompt_store.py`：覆盖图片魔数校验、超限拒绝、删除连带删图、导出导入往返（保留原顺序）、重复与非法条目跳过、非法时间戳回退等 14 个用例。

- 新增外观模式配置：助手设置里可切换「跟随 ComfyUI ／ 暗色 ／ 亮色」，默认跟随——ComfyUI 用什么主题插件就用什么主题。此前插件把配色硬编码成暗色值，在 ComfyUI 亮色模式下文字与背景几乎无法分辨。
- 实现上新增一组 `--bpi-*` CSS 变量（面板、文字、边框、按钮、输入框、表格、弹窗），按 `:root`（亮色默认）、`.dark-theme`（跟随 ComfyUI 暗色）、`[data-bpi-theme]`（强制暗／亮）四套定义；配置存在 `assistant_store` 的 `appearance` 字段，切换后即时生效无需刷新。
- 节点中文同步编辑区的「标签管理」「助手设置」入口已移除（与侧边栏管理面板重复），代码里保留注释说明如何恢复；助手设置里的翻译服务说明改为整行显示，不再挤在表单左列。

- 用户界面英文化：为符合 ComfyUI-Manager 注册要求，节点显示名改为 `Prompt Translator & Manager`，`@title` / `@description` / `RETURN_NAMES` / `CATEGORY` 与约 540 处 JS 界面文字（按钮、提示、状态消息、输入框占位符、语法诊断）全部改为英文。
- 中文通过 locale 保留：新增 `locales/zh/main.json`（`nodeDefs` 结构），在 ComfyUI 里把界面语言切到中文后，节点名、描述与输入输出名仍显示中文「提示词翻译与管理（英文输出）」——由 ComfyUI 的 `build_translations()` 与前端 `mergeCustomNodesI18n()` 自动合并，无需额外配置。
- 英文化时有四类内容刻意保留中文：代码注释；匹配用户中文输入的正则模式（如「翻译要求／正文」结构化指令识别）；词库中文分类名匹配表（改掉会导致分类排序失效）；以及「未收录」「自然语言片段（待翻译或确认）」等在中文列显示的翻译数据。
- 更新 5 个测试文件的断言以匹配新的英文字符串（`test_anima_sorter.mjs`、`test_clear_controls.mjs`、`test_dictionary_tools.mjs`、`test_parser.mjs`、`test_project_link.mjs`）。

- 新增界面语言设置：助手设置页新增「Language」下拉，可选「跟随 ComfyUI 语言设置」（默认）／「中文」／「英文」，随其它设置一起存在 `assistant_store` 的 `language` 字段（auto / zh / en），并在 `localStorage` 留一份供首屏同步取值。
- ComfyUI 自带的 locale 只翻译 `nodeDefs`（节点名、描述、输入输出名），面板里的按钮、状态栏、tooltip 管不到，因此新增插件自建的 i18n 层 `js/i18n.js`：源码继续保留英文原文（上架要求），运行时按原文查中文表，查不到就原样显示英文，不会出现半中半英或空文案。
- 文案统一走 `t()`：`element()` / `button()` / `field()` 三个 DOM 工厂与全部 `title` / `placeholder` / `textContent` 赋值点都接入翻译（共 200+ 条文案）。带变量的文案（如 `deleted “xxx”; press Ctrl+Z to undo`）把「“…”」片段和数字折叠成 `{}` 占位符再回填，模板字符串也能整句翻译。
- 切换语言即时生效：创建元素时把英文原文记在 `data-bpi-i18n-*` 属性上，改语言后整树重刷；状态栏等动态文案会摘掉标记，不会被回填成旧内容。个别仍停留在旧语言的位置刷新页面即可。
- 顺带修掉英文化时留下的一批残损文案（缺少空格的「“确认清空”」、「Weight must be 0 to 3  to 3」、重复的「Ctrl+Z ; Ctrl+Z」等约 35 处）。
- 新增 `tests/test_i18n.mjs`：覆盖强制英文／中文、未翻译文案回退、变量回填、非法值退回 auto、auto 读取 `Comfy.Locale`（zh-CN / zh-TW / en）与读取失败不崩等断言。
- 补齐第二批翻译（约 200 条）：词包卡片信息（个人词库／大词库／社区词包的条数、版本、来源、授权）、标签管理的统计行「共 N 个标签｜已识别｜未收录｜待确认」、模式提示、各类操作回执（移动／隐藏／恢复／翻译／导入）、语法诊断全部文案（括号、权重、重复标签、冲突组、输入模式判定理由）、Anima 分类名、以及所有请求失败提示。
- 变量的递归翻译：嵌在文案里的变量本身也可能是界面词（如冲突组名 `Solo vs multiple subjects`、模式名 `Tag`），回填时会再翻译一层，避免出现「中文句子里夹一个英文词」。
- 为让变量能被稳定折叠，`parser.js` 的诊断模板与词包信息模板给动态片段加上「“...”」包裹（版本号 `1.0.0`、来源、授权等），否则带点号的版本会被拆成多个变量而匹配不到译表。

## v1.1.0（2026-09-18）

- 新增面向全部启用词库的概念模糊搜索、40 项分页、“搜索更多”和结果结束提示。
- 未知标签可直接搜索候选或手动加入个人词库。
- 新增 LM Studio 本地预设并修正兼容接口地址拼接。
- 中文／混合文本编辑区支持高度调整、持久化和展开编辑。
- “按官方顺序整理”支持分类换行，保留自然语言、权重和分段结构。
- 修复“搜索更多”后列表跳回顶部；同一查询会保持原浏览位置。
- 分类整理后的中文联动区改为带分类名称的两列表格，分类名称不会写入实际提示词。
- 英文实际输出区新增二次确认清空按钮；确认清空后按钮变为“撤销”，同时支持 `Ctrl+Z`。中文／混合草稿区不设置重复的清空入口，返回联动即可查看英文输出对应的中文视图。
- 适配 ComfyUI 0.36.0 现代节点布局接口；顶部 ComfyUI 原始英文文本框保持可见，标签管理展开／收起时节点重新适配内容高度。
- 发布包不再携带空个人词库或默认运行设置，避免覆盖升级时清空 `user_tags.json`、词库启停选择和大型词库选择；构建脚本新增旧版覆盖升级哨兵测试。

## v1.0.0

- 首个公开正式版本。
